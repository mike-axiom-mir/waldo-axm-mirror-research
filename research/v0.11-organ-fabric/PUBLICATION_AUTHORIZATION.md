# v0.11 publication authorization

Status: `PUBLIC AXM RESEARCH / EXPERIMENTAL`

The original `axm-fabrics-current-waldo-2026-08-23.zip` packet was captured with a conservative request boundary that denied `PUBLISH` and runtime execution. Those original request/receipt bytes are preserved unchanged as historical evidence.

After the packet was supplied, Mike explicitly authorized public GitHub publication of AXM material on 2026-08-23 and separately authorized unrestricted experimentation inside the detached Waldo research lane. That later authorization changes the distribution and experiment boundary from that point forward; it does **not** retroactively rewrite the original packet request or any earlier receipt/hash.

This checkpoint therefore publishes:

- the exact source packet in reconstructable byte-identical chunks;
- the original static v0.11 bridge study unchanged;
- the later authorized disposable runtime observation and compact runtime receipt;
- a WALDO/Mirror verifier/witness for that runtime receipt.

Nothing here is an OpenWALDO upstream contribution. Nothing is installed, registered, staged, promoted, merged into AXM platform `main`, or made CANON merely by publication.
