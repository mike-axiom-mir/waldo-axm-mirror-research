{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "axm.local-continuation-capsule/v1",
  "type": "object",
  "required": ["schema", "state", "declarationDigest", "identity", "currentGoal", "sources", "freshness", "truth", "blockers", "capsuleDigest"],
  "properties": { "schema": { "const": "axm.local-continuation-capsule/v1" }, "state": { "enum": ["READY", "HELD"] }, "capsuleDigest": { "type": "string", "pattern": "^[a-f0-9]{64}$" } },
  "additionalProperties": true
}
