# Keel Workshop collaborator shell trial

Status: `EXPERIMENTAL` · human review required

This is an AI-proposed origin blueprint for the Workshop-assigned **Keel** role with a disclosed **Codex technical substrate**. It is a test of the Identity Shell Fabric, not a claim that the manifest is a person, a conscious subject, an active runtime, an authenticated human decision, or proof of continuity between sessions or models.

The shell deliberately keeps these layers separate:

```text
Keel role contract != Codex/model runtime != coding capability != software body
```

The reasoner and body descriptors are reused by exact digest from the neutral v0.1 fixtures. They remain unbound and inert. No connector, model, tool, filesystem, network, or body is loaded by compilation. The declared resource envelope is a ceiling for this inert composition, not a measurement or promise about the active Codex host.

No continuity or memory receipt is accepted at origin. A future retained memory must enter as `CANDIDATE` and receive an allowed human acceptance receipt. A different model or connector requires a disclosed migration; the Keel name alone is not continuity evidence.

Run the fit and rebuild check:

```powershell
node shared/identity-shell-fabric/examples/keel-workshop-collaborator/trial-selftest.js
```

The committed compiled artifacts are regenerated in memory and compared byte-for-byte. The trial also proves that a copy must become a distinct fork, generated memory stays candidate until an exact acceptance event, and a neutral neural descriptor migration must disclose the adapter/model-or-connector change and its resource request. The script performs no writes.

Run the independent verifier in a fresh Node process without importing the compiler:

```powershell
node shared/identity-shell-fabric/examples/keel-workshop-collaborator/fresh-process-verifier.js
```

That fresh process accepts the committed manifest and rejects three maliciously re-digested variants: unsafe portability, false empty continuity with accepted history, and numeric-string resource coercion.

Run the standalone receipt verifier in another fresh Node process:

```powershell
node shared/identity-shell-fabric/examples/keel-workshop-collaborator/fresh-process-receipt-verifier.js
```

It accepts the committed build/gap and lineage receipts without importing the compiler, then rejects four maliciously re-digested receipt variants. Its evidence ceiling is static receipt verification; it does not authenticate a human or establish runtime continuity.
