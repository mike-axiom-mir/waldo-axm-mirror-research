# Deterministic Adventure Content Generator v1

Status: `TEST`

This is the next bounded rung after the first generated Four Roots Run maze. It
does not generate or execute arbitrary code. It validates one versioned native
adventure recipe and emits a byte-bound content packet plus a non-writing Game
Hub installation plan.

The packet preserves the exact v0.1 ancestor, Mike's explicit internal `TEST`
direction, four technical-root `PASS` evidence references, resource ceilings,
and the unresolved public-reuse hold. The generator reads only its declared
recipe, calls no provider, writes nothing, runs no game code, uses no network,
and cannot install, integrate, promote, publish, alter Foundation, or change
`CANON`.

The trusted Game Hub shell is a separate reviewed component. It consumes the
exact content bytes only after the host applies Mike's bounded decision. Future
content releases can remain immutable alternatives without silently replacing
the running release.

Run:

```powershell
node shared/code-capability-fabric/selftest-deterministic-adventure-content-generator-v1.js
```
