# v0.49 verification receipt

The final commit is created only after the exact donor digest is sealed and synthetic-frame tests prove host-source refusal, bounded frame eviction, byte accounting, metadata without payloads, sealed SHA-256 receipt, no raw video archive, DOM-required contact sheet, cleanup/wipe, closed-buffer refusal, and no automatic publishing.
